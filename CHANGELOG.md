# 更新日志

## v0.2.10 - 2026-05-26

这是一次面向后台驻留内存的性能优化版本。

### 后台内存

- 新增三阶段后台释放策略：关闭窗口后先进入 warm 阶段释放完整内存索引，再进入 deep 阶段释放可见结果和搜索上下文，最后进入 cold 阶段只保留后台恢复所需状态。
- 前台恢复继续采用轻量预览优先，再后台加载完整索引，兼顾快速唤起和长期低内存驻留。
- 后台阶段会保留全局快捷键、FSEvents 事件缓冲和必要配置，不影响后台唤起与文件变化记录。
- 诊断报告新增后台内存阶段，方便后续排查驻留内存状态。

### 发布修复

- 打包脚本只识别 `v` 开头的版本标签，避免快照标签污染应用版本号。
- 删除临时快照标签，避免公开标签列表混乱。

### 验证

- 本地测试覆盖提升到 54 个用例。
- 已验证 `swift test`、`NeedleCoreCheck`、本地安装、签名校验和运行态后台内存下降。

## v0.2.9 - 2026-05-26

这是一次面向公开展示和正式分发节奏的说明文案版本。

### 产品说明

- 重写 GitHub 首页说明，将 Needle 定位为成熟的 macOS 原生文件搜索工具，而不是开发预览项目。
- 补充适用场景、功能概览、快速开始、权限说明、隐私边界和发布状态。
- 强化“独立索引、中文优先、低打扰后台运行、本地隐私”的产品卖点。

### 发布

- 更新应用版本号到 0.2.9。
- 重新生成 DMG 安装包，并同步 GitHub Release。
- 更新 GitHub About 描述，让仓库首页和项目定位保持一致。

## v0.2.8 - 2026-05-26

这是一次面向冷启动体验、搜索稳定性和首页文案成熟化的版本。

### 启动体验

- 冷启动改为分阶段加载：先显示可用界面，再后台装载完整内存索引，减少窗口打开时的卡顿。
- 用户在索引未完全装载时输入搜索，会进入等待状态，避免展示半成品结果。
- 全量索引完成后会自动收敛到当前查询，不会覆盖用户输入。

### 搜索稳定性

- 新查询开始时立即清空旧结果，避免上一轮内容短暂残留到新搜索里。
- 保持“正在搜索 / 已找到 / 无结果”的状态切换稳定一致。

### 首页与说明

- 将 GitHub 首页说明改为成熟产品介绍，突出 Needle 的定位、使用方式、权限边界和隐私原则。
- 去掉首页中的开发预览语气，只保留对用户有价值的产品信息。

## v0.2.7 - 2026-05-25

这是一次针对个人目录大范围索引场景的稳定性与性能修复版本。

### 修复

- 修复主窗口列表在静置或滚动过程中偶发变成空白的问题。
- 修复后台索引内存卸载时会把当前可见结果一起清空的问题；现在只释放全量索引，保留轻量可见结果快照。
- 修复默认列表被文件存在性自愈逻辑误清空的问题；显式搜索已删除文件时仍会及时从结果中移除。

### 性能

- 搜索上下文未变化时跳过重复搜索，减少输入框、筛选和 UI 状态变化造成的重复计算。
- 降低后台窗口可见性巡检频率，减少后台空转唤醒。
- 结果列表增加等价性判断，降低无意义重绘造成的卡顿。
- 诊断报告新增搜索执行次数和跳过次数，便于排查 CPU 占用。

### 索引规则

- 默认排除低价值高噪声目录：`.DS_Store`、`Library/Caches`、`Library/HTTPStorages`、`Safari/Favicon Cache`、`.Trash`、`node_modules`、`.build`。
- 已保存旧默认排除项的用户会自动迁移到新默认值；用户自定义过的排除项不会被覆盖。
- 保留 `Library/Preferences` 的索引能力，方便高级用户搜索配置文件。

### 验证

- 新增默认列表稳定性、后台卸载可见结果保留、旧默认排除项迁移和搜索去重相关测试。
- 本地测试覆盖提升到 45 个用例。

## v0.2.0 - 2026-05-25

这是一次以搜索可靠性、性能和 macOS 体验打磨为核心的预览版更新。

### 搜索与索引

- 改进用户目录扫描逻辑，针对 `Desktop`、`Documents`、`Downloads`、`Movies`、`Music`、`Pictures` 等受保护目录进行显式子扫描，减少“目录存在但子文件未进入索引”的情况。
- 修复重建索引完成提示过早的问题，确保当前查询结果刷新完成后再回到监听状态。
- 优化搜索任务调度，后台 FSEvents 增量刷新不再反复触发前台“正在搜索”动画。
- 增强搜索语法，支持 `.ext`、`ext:ext`、通配符和 `re:` 正则，并对无效正则给出提示。
- 优化排序逻辑，文件夹、文件、其他类型按更稳定的分组排序，并保留名称、类型、大小、修改时间排序入口。

### 预览与详情

- 右侧详情栏改为按窗口比例分配默认宽度，并保留用户手动拖动后的宽度。
- 详情信息精简为位置、大小、访问权限、创建时间、修改时间、最近打开等高价值字段。
- 位置字段显示压缩路径，悬停时弹出完整路径，避免长路径挤压详情卡片。
- 小型文本文件支持直接在右侧预览区选择和复制文本。
- 文件夹大小改为选中后异步计算，避免对未选中文件夹做昂贵统计。
- 文件图标改为优先使用系统文件/类型图标，减少通用图标出现。

### 界面与交互

- 优化设置面板布局、分区密度、说明提示和权限引导。
- 改进搜索状态显示：空查询显示就绪，用户查询中显示搜索动画，完成后显示已找到或无结果。
- 添加右键菜单中的打开方式、Quick Look、Finder 显示、复制路径等常用操作。
- 增加可拖动的结果列表/预览栏分隔条，双击可恢复默认比例。
- 关闭窗口和后台运行行为更贴近常用文件管理场景。

### 质量与发布

- 添加并扩展查询解析、排序、SQLite、索引刷新和重建完成时机相关测试。
- 更新 DMG 打包、Developer ID 签名和 notarization 脚本骨架。
- 保持 AGPL-3.0 开源协议。
